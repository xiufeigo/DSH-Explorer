# Same insert as cordis.patch.yml — copy into a profile's cordis.patch.yml
# only when installing by hand. Prefer `dsh plugin add` (reads dsh.bundle) or
# `pnpm plugin:install` (junction shortcut).

- insert:
    # Dual-face plugin: Host registers POST /dsh-explorer/rpc and /pty; the
    # browser half is scanned from package.json `dsh.client`.
    - id: dsh-explorer
      name: dsh-explorer
